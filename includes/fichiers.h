#ifndef FICHIERS_H
#define FICHIERS_H

/**
 * @brief sauvegarde la map dans un fichier
 * 
 * @param map map à sauvegarder
 * @param size taille de la map
 * @return 1 si la sauvegarde a bien été faite, sinon 0
 */

int sauvegarde(int **map, int size);

#endif